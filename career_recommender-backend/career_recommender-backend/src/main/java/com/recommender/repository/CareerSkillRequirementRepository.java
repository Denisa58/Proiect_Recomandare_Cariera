package com.recommender.repository;

import com.recommender.model.CareerSkillRequirement;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerSkillRequirementRepository extends JpaRepository<CareerSkillRequirement, Long> {

}